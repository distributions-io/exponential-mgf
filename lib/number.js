'use strict';

// FUNCTIONS //


// MGF //

/**
* FUNCTION: mgf( x, lambda )
*	Evaluates the moment-generating function (MGF) for a exponential distribution with rate parameter `lambda` at a value `t`.
*
* @param {Number} t - input value
* @param {Number} lambda - rate parameter
* @returns {Number} evaluated MGF
*/
function mgf( t, lambda ) {

} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;
